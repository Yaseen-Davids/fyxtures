{
  "version": 2,
  "rewrites": [{ "source": "/ui/(.*)", "destination": "/" }],
  "builds": [
    {
      "src": "api/src/Server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "api/src/Server.ts" }]
}
